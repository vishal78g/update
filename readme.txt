Simple Website

git steps
1. craete repository
2. copy code from option (or push an existing repository from the command line)
3. 

git local repository 
1. initialise by git init in command line
2. check the status of your working directory by writing git status
3. add to staging area by writing git add filename 
4. once the file is added to staging area check status the file will shown in green color
5. then write git commit -m"add coments to add specific details about commit"
6. once the commit is completed then its time to connect to remote repository
7. write git remote add origin and the link of your repository
8. git push -u origin main (to add files from main brach to  remote git)
9. check your git repository the files should be updated now 